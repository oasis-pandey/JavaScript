"use strict"; //treat asll JS code as newer version

// alert("Hello") // we are using nodejs, not browser, so you get an error

console.log(3+3)
console.log("Oasis")

/*
    string = "oasis";
    number/ integer = 5
    bool = true/false
    null = standalone value
    undefined => not defined 
    symbol = unique stuff
*/

console.log(typeof(5))